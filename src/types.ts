// Basic form data interface for Google Sheets
export interface FormData {
  email: string;
  company: string;
  role: string;
  usecase: string;
  investment: string;
  transactionHash: string;
  donationAmount: string;
}